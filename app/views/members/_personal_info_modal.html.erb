<%= form_with model: current_user, url: update_member_personal_details_path, method: :patch do |form| %>
  <div class="modal-body">
    <div class="row">
      <div class="col-lg-12">
        <div class="form-group row">
          <div class="col-lg-6">
            <%= form.label :first_name, class: "mb-3 mt-3" %>
            <%= form.text_field :first_name, class: "form-control", value: current_user.first_name %>
          </div>
          <div class="col-lg-6">
            <%= form.label :last_name, class: "mb-3 mt-3" %>
            <%= form.text_field :last_name, class: "form-control", value: current_user.last_name %>
          </div>
        </div>
      </div>

      <div class="col-lg-12">
        <div class="form-group row">
          <div class="col-lg-6">
            <%= form.label :city, class: "mb-3 mt-3" %>
            <%= form.text_field :city, class: "form-control", value: current_user.city %>
          </div>
          <div class="col-lg-6">
            <%= form.label :state, class: "mb-3 mt-3" %>
            <%= form.text_field :state, class: "form-control", value: current_user.state %>
          </div>
          <div class="col-lg-6">
            <%= form.label :country, class: "mb-3 mt-3" %>
            <%= form.text_field :country, class: "form-control", value: current_user.country %>
          </div>
          <div class="col-lg-6">
            <%= form.label :pincode, class: "mb-3 mt-3" %>
            <%= form.text_field :pincode, class: "form-control", value: current_user.pincode %>
          </div>
        </div>
      </div>

      <div class="col-lg-12">
        <div class="form-group">
          <%= form.label :profile_title, class: "mb-3 mt-3" %>
          <%= form.text_field :profile_title, class: "form-control", value: current_user.profile_title %>
        </div>
      </div>
    </div>
  </div>
  <div class="modal-footer">
    <%= form.submit 'Save Changes', class: 'btn btn-primary', data: { action: 'click->bs-modal#submitEnd' } %>
    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
  </div>
<% end %>
